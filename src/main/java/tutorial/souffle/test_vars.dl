.decl var(x: symbol)
var("x").
var("y").

.decl var2(x: symbol)
var2("x").

.decl var3(x: symbol)
var3("j").

.decl foo(z: symbol)
foo("o").
foo(z) :- var(z), var2(z).
foo(z), var3(z) :- var3(z).
.output foo
